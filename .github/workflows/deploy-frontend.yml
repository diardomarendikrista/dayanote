name: Deploy Frontend to Windows Server

on:
  push:
    branches:
      - main
    paths:
      - "frontend/**"
      - ".github/workflows/deploy-frontend.yml"

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Update Frontend Code (Pull Only)
        run: |
          echo "Frontend code updated via git checkout. Skipping build and restart as requested."
